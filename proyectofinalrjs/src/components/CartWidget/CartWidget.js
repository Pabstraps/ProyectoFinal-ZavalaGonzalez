import React from 'react'
import Button from 'react-bootstrap/Button';


function BotonCarrito() {
      return (
        <>
          <Button variant="primary">Carrito de compras</Button>{' '}
        </>
      );
    }
    
    export default BotonCarrito;
